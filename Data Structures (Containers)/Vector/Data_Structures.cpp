#include <vector>		// vector library
#include <list>			// list library
#include <set>			// set library
#include <map>			// map library
#include <stack>		// stack library
#include <queue>		// queue library


#include<iostream>
using namespace std;

int main() {
	
	cout << "Vector: \n";
	cout << "Stores elements like an array but can dynamically change in size. "
		 << "Adding and removing of elements are usually done at the end. "
		 << "Elements can be accessed by index.";
	

}

